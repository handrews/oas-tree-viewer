openapi: 3.1.0
$self: https://example.com/oad/dynamicref-remote
info:
  title: $dynamicRef — unreachable document
  version: 1.0.0
  description: >
    Not referenced from the entry, so it is unreachable. Its $dynamicAnchor "item" can never be in
    an entry-rooted dynamic scope, so it is excluded from GenericList's tentative #item targets.
paths: {}
components:
  schemas:
    RemoteList:
      $id: https://example.com/schemas/remotelist
      $dynamicAnchor: item
      type: object
