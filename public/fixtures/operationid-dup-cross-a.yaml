openapi: 3.2.0
$self: https://example.com/oad/operationid-dup-cross-a
info:
  title: Cross-document duplicate operationId — entry
  version: 1.0.0
paths:
  /a:
    get:
      operationId: sharedOp # unique within THIS document
      responses:
        '200':
          description: OK
