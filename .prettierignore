# Build output, deps, and test artifacts.
dist
coverage
node_modules
playwright-report
test-results

# Sample documents / served assets — not source to format.
public

# Hand-written docs (kept hand-formatted) and the npm-managed lockfile.
*.md
package-lock.json
